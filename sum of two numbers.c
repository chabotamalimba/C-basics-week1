#include <stdio.h>   // Allows us to use printf()

int main() {
    int a = 10;          // First number
    int b = 20;          // Second number
    int sum = a + b;     // Add a and b, store result in sum

    printf("Sum is %d", sum);   // %d displays an integer value

    return 0;   // End of program
}